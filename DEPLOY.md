# 🚀 部署 Server 模式示範到 Zeabur

## 📋 部署步驟

### 步驟 1：在 GitHub 創建新 Repository

1. 前往 https://github.com/new
2. Repository name: `server-demo`
3. 選擇 Public 或 Private
4. **不要勾選** "Add a README file"
5. 點擊 "Create repository"
6. 複製 repository URL（例如：`https://github.com/g83204/server-demo.git`）

### 步驟 2：推送到 GitHub

執行 `deploy-to-github.bat` 並貼上您的 GitHub Token

### 步驟 3：在 Zeabur 部署

1. 登入 https://zeabur.com
2. 點擊 "New Project"
3. 選擇 "Deploy from GitHub"
4. 選擇 `server-demo` repository
5. Zeabur 會自動偵測為 Astro 專案
6. 等待部署完成（2-3 分鐘）

### 步驟 4：設定域名

1. 在 Zeabur 專案中，點擊服務
2. 找到 "Networking" 或"網路"標籤
3. 點擊 "Generate Domain" 生成域名
4. 獲得 URL（例如：`https://server-demo-xxx.zeabur.app`）

### 步驟 5：測試

訪問您的 URL，應該能看到：
- ✅ 即時的伺服器時間
- ✅ 訪客資訊
- ✅ API 路由正常運作

每次重新整理，時間都會更新！

---

## ⚙️ Zeabur 配置說明

Zeabur 會自動：
- 執行 `npm install`
- 執行 `npm run build`
- 啟動 Node.js 伺服器

不需要額外配置！

---

## 🎯 驗證 Server 模式

部署成功後，測試以下功能：

1. **動態時間**
   - 重新整理頁面
   - 時間應該會更新

2. **API 路由**
   - 訪問 `https://your-url.zeabur.app/api/time`
   - 應該回傳 JSON 格式的時間

3. **訪客資訊**
   - 頁面應該顯示您的 IP
   - 顯示瀏覽器資訊

---

## 💡 與 Static 模式的對比

**您的書局網站（jackdemo）：**
- Static 模式
- 內容固定
- 超快速度

**這個示範（server-demo）：**
- Server 模式
- 動態內容
- 即時更新

兩個都在 Zeabur 上運行，可以直接對比差異！

---

## 🎉 完成

部署成功後，您就有兩個網站在 Zeabur 上：
1. `jackdemo` - Static 模式（書局業務網站）
2. `server-demo` - Server 模式（動態示範）

可以直接體驗兩種模式的差異！
